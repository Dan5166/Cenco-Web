import { ShowForRolesDirective } from './show-for-roles.directive';

describe('ShowForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForRolesDirective();
    expect(directive).toBeTruthy();
  });
});
